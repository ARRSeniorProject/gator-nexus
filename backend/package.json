{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for gator-nexus",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "NODE_ENV=production node server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix ./../frontend && npm run build --prefix ./../frontend"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.11.18"
  }
}
